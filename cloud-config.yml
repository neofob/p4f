#cloud-config

package_update: true
package_upgrade: true

packages:
  - python3
  - python-apt
  - python-pip
  - mc
  - vim
  - sysstat

power_state:
  delay: "now"
  mode: reboot
  message: Rebooting
  condition: True
